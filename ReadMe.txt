Для просмотра страници входа, нажмите "Sign in"

Классы "input2" "submit2" были созданы для изменения цвета фона "placeholder" в инпуте при нажатии "Sign in"

При разработке сайта использовал разные техники для выполнения похожих задач (просто для наглядного примера возможностей). Например добавлении иконок с помощью сторонних ссылок и локальных, а так же исользование шрифтов локально и при помощь ссылок. 

To view the login page, click "Sign in"

The "input2" "submit2" classes were created to change the background color of the "placeholder" in the input when clicking "Sign in"

When developing the site, I used different techniques to perform similar tasks (just for an illustrative example of the possibilities). For example, adding icons using third-party links and local ones, as well as using fonts locally and using links.